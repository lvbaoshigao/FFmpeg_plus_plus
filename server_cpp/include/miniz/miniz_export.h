/* miniz_export.h —— miniz 仓库中此文件由 CMake 生成（仓库内不存在），
 * 静态并入 ffmpegpp DLL 无需 dllexport 修饰，这里提供等效的空导出宏。 */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

#define MINIZ_EXPORT
#define MZ_EXPORT

#endif /* MINIZ_EXPORT_H */
